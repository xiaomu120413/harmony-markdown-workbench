# ADR-003：内部库和外部 URI 双模式

- 状态：建议接受，M0-02 后确认。
- 背景：移动端无法完全复制桌面任意目录工作区；系统文件选择器（SAF 类）访问外部文件需要持久化 URI 权限。
- 决策：区分 `internal`（应用私有目录，相对路径）与 `external`（持久化 URI + 显示名）两种来源；适配器实现统一 `DocumentFileAdapter` 契约。
- 后果：需要向用户解释两种来源与权限差异；若持久权限不可用，必须显示"临时访问"并支持重新定位。
- 关联：FR-FILE-001、FR-FILE-002；M0-02 / M1-01。