# ADR-004：首版不做云同步

- 状态：接受。
- 背景：降低账号、冲突、隐私与服务端成本；V0.1 定位完全离线。
- 决策：V0.1/V0.2 不自建同步服务、不要求登录；跨设备依赖系统分享或用户现有文件同步方案。WebDAV/Git/云空间等列为 V1.0 后候选（SDD 3.3）。
- 后果：无多端自动同步能力；冲突域限定在外部程序对同一文件的修改。
- 关联：SDD 3.4；6.3。